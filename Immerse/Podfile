platform :ios, '9.1'
use_frameworks!
pod 'RealmSwift'
pod 'KYDrawerController'
pod 'XLPagerTabStrip', '~> 5.0'
pod 'UITags'

source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '9.1'
use_frameworks!

# @TODO: At some point we'll expand to use these as well, but for the time being
# they aren't exactly very useful. Commented out for now until we find a need.
#
# pod 'SDWebImage'
# pod 'Alamofire'
# pod 'CNPPopupController'
